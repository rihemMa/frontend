import { PaperPipePipe } from './paper-pipe.pipe';

describe('PaperPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PaperPipePipe();
    expect(pipe).toBeTruthy();
  });
});
