import { ProjectPipePipe } from './project-pipe.pipe';

describe('ProjectPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectPipePipe();
    expect(pipe).toBeTruthy();
  });
});
